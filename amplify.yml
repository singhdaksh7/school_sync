version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --cache .npm --prefer-offline
        - npx prisma generate
        - echo "DATABASE_URL=$DATABASE_URL" > .env.production
        - echo "AUTH_SECRET=$AUTH_SECRET" >> .env.production
        - if [ -n "$AUTH_URL" ]; then echo "AUTH_URL=$AUTH_URL" >> .env.production; fi
        - echo "NEXTAUTH_SECRET=$AUTH_SECRET" >> .env.production
        - if [ -n "$NEXTAUTH_URL" ]; then echo "NEXTAUTH_URL=$NEXTAUTH_URL" >> .env.production; fi
        - echo "ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY" >> .env.production
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - .npm/**/*
